#include <stdio.h>
#include<math.h>
#include<stdlib.h>
int main(){
 float X1, Y1, P1;
 float X2, Y2, P2;
 float G, D, C, F;
 scanf("%f %f %f %f", &X1, &Y1, &X2, &Y2);
 P1=(X2-X1);
 P2=(Y2-Y1);
 G= sqrt(P2*P2+P1*P1);
 printf("%.4f\n",G);
 system("pause");
return 0;
}
